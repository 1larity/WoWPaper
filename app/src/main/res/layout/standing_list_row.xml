<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:cacheColorHint="@android:color/transparent"
    android:clickable="false"
    android:orientation="horizontal"
    android:paddingBottom="1dp"
    android:paddingLeft="10dp"
    android:paddingRight="0dp"
    android:paddingTop="1dp" >

    <TextView
        android:id="@+id/cTeamName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:singleLine="true"
        android:text="League Name"
        android:textSize="12sp"
        android:textStyle="bold"
        android:layout_alignEnd="@+id/cTeamDetails"
        android:layout_alignRight="@+id/cTeamDetails" />

    <TextView
        android:id="@+id/cTeamDetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/cTeamName"
        android:text="Content"
        android:textSize="12sp"
        android:layout_toStartOf="@+id/cRank"
        android:layout_toLeftOf="@+id/cRank"/>

    <TextView
        android:id="@+id/cRank"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Content"
        android:textSize="24sp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:paddingRight="8sp"
        android:layout_centerVertical="true"
        android:paddingLeft="8sp" />

</RelativeLayout>